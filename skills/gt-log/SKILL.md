---
name: gt-log
version: 1.0.2
description: Show the current Graphite stack with PR status
allowed-tools: Bash(gt log *)
---

# Show Graphite Stack

Display the current stack of branches and their PR status.

## Steps

1. Run `gt log --stack` to show the current stack with PR details
2. Summarize the stack state (which PRs are open, merged, need review, etc.)
